# There's a built in kron function, let's assign it to ⊗

const ⊗ = kron

println(ones(2) ⊗ ones(3)) # Results in length 6 array

function test()
    println("Test")
    4
end

test()

println("HI")

println("WHy is atom so slow?")
